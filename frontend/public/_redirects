# Netlify redirects for React Router (SPA)
# This file ensures all routes work properly with React Router

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# API proxy (optional - if you want to proxy backend requests)
# /api/*  https://trip-compare.onrender.com/:splat  200
